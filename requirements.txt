# required
httpx>=0.27
# optional: better URL extraction (falls back to a plain tag strip without these)
trafilatura>=1.8
beautifulsoup4>=4.12
markdownify>=0.13
# optional: .docx draft support
python-docx>=1.1
